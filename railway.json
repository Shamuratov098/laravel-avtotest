{
    "build": {
        "builder": "NIXPACKS"
    },
    "deploy": {
        "startCommand": "php artisan migrate --force && php artisan storage:link && npm run build && php artisan config:cache && php artisan route:cache && php -S 0.0.0.0:$PORT -t public"
    }
}
